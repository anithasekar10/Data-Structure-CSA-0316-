#include <stdio.h>
int main() {
    printf("Name: Anitha\nReg no: 192324040\n");
    int n,f=1;
    printf("Enter the number: ");
    scanf("%d", &n);
    int temp=n;
    while(temp>=1){
    	f=f*temp;
    	temp--;
	}
	printf("\nThe factorial of %d is %d",n,f);
    return 0;
}
